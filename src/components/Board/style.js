import createStyles, { fonts, colors, flexContainer } from '../../styles/index.js';

const boardStyles = createStyles({
  flexContainer: {
    ...flexContainer,
    backgroundColor: 'yellow'
  }
})

export default boardStyles;
